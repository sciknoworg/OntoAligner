version: "2"


build:

  os: "ubuntu-22.04"
  tools:
    python: "3.9.16"

python:
  install:
    - requirements: docs/requirements.txt
    - requirements: requirements.txt

sphinx:
  builder: html
  configuration: docs/source/conf.py

submodules:
  include: all
